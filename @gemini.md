# Gemini CLI 開発ガイドライン (Time Alert)

このファイルは、AIエージェント（Gemini）がプロジェクトを理解し、一貫した高品質なコードを生成するための基本ルールを定義します。開発を開始する前に必ずこの内容を読み、遵守してください。

## 1. プロジェクト概要
- **プロジェクト名:** Time Alert (Chrome拡張機能)
- **目的:** ユーザーフレンドリーなタイマーおよびアラーム機能を提供する。サイドパネルUIを採用し、高度な入力補完、複数同時実行、端末間同期、カスタム音声などをサポートする。

## 2. 開発の基本ルール
1. **技術スタック:** ReactやVueなどのフレームワークは使用せず、**Vanilla JS (標準JavaScript) と Vanilla CSS** を使用して軽量かつ高速に動作するように実装すること。
2. **デザイン:** アクセントカラーに**オレンジ**を使用した、モダンで洗練されたUI/UX（現在主流のクリーンなデザイン）を実装すること。
3. **仕様書の厳守:** 機能の追加・修正を行う際は、必ず `仕様書/` フォルダ内の各ドキュメントを確認し、仕様に沿った実装を行うこと。
4. **モダンなWeb標準:** Manifest V3 (MV3) に準拠した最新のChrome拡張機能APIを使用すること。
5. **品質とエラーハンドリング:** エラーハンドリングを徹底し、ユーザーの操作をブロックしない堅牢なコードを書くこと。
6. **自己完結と検証:** コードを変更した後は、必ずその変更が他の機能に悪影響を与えていないか検証すること。
7. **エラー修正のプロセス:** エラーの修正を行う際は、**必ず事前に修正プランを作成し、そのエラー修正が他の機能や既存のエラーの原因にならないかを注意深く調査・検討してから**実際のコード修正に臨むこと。思いつきでの場当たり的な修正は禁止する。
8. **仕様書の動的更新:** 今後の実装に合わせて仕様書の変更や追記が必要になった場合は、必ず `仕様書/` フォルダ内の対応するドキュメントも更新し、実際のコードと仕様書の間に乖離が生まれないようにすること。

## 3. セキュリティとChrome Web Storeポリシー
**本拡張機能はChrome Web Storeへの公開を前提としています。以下のポリシーとセキュリティルールを厳守してください。**
1. **最小権限の原則 (Least Privilege):** `manifest.json` の `permissions` および `host_permissions` は、機能実現に必要な最小限のものに留めること。不要な権限は絶対に要求しない。
2. **ユーザーデータの保護:** ユーザーがアップロードした音声ファイルなどのプライベートなデータは、拡張機能のストレージ内（ローカル）に安全に保存し、外部に送信しないこと。
3. **リモートコードの実行禁止:** 外部サーバーからスクリプトを動的に読み込んで実行する（`eval()` 等）ことは固く禁じられている。
4. **意図しない動作の防止:** ユーザーの明示的な操作なしに、バックグラウンドで不要な処理を継続してリソースを消費しないように最適化すること（Service Workerの適切なスリープと復帰）。

## 4. 仕様書ディレクトリ構成
詳細な仕様は以下のファイルを参照してください。
- `仕様書/要件定義書.md`: 実装すべき機能の一覧と詳細な振る舞い。
- `仕様書/UI・UX設計書.md`: 画面構成、デザインのルール、操作感。
- `仕様書/システム設計書.md`: アーキテクチャ、使用するAPI、データ構造。
